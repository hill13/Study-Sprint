# Web Framework
fastapi==0.109.0          # The main web framework - fast, modern, async Python
uvicorn==0.27.0           # ASGI server to run FastAPI

# Database
sqlalchemy==2.0.25        # ORM - lets you work with database using Python objects
psycopg2-binary==2.9.9    # PostgreSQL driver for Python
alembic==1.13.1           # Database migrations - version control for your schema

# Authentication
python-jose[cryptography]==3.3.0  # JWT token creation and validation
passlib[bcrypt]==1.7.4    # Password hashing (secure storage)
bcrypt==4.0.1             # Pinned for passlib compatibility

# Validation & Settings
pydantic==2.5.3           # Data validation (already included with FastAPI)
pydantic-settings==2.1.0  # Load settings from .env files
python-dotenv==1.0.0      # Read .env files
email-validator==2.1.0    # Email validation for Pydantic EmailStr
python-multipart==0.0.6   # Form data parsing for OAuth2

# Development
httpx==0.26.0             # For testing async endpoints

# API
openai>=1.0.0                 # OpenAI API SDK